2. Не выполняя кода, ответить, что выведет браузер и почему:
if (!("a" in window)) {
    var a = 1;
}
alert(a);
/* Вернёт undefine. Переменную "a" он видит при первичном "скане", но без конкретного значения.*/


var b = function a(x) {
    x && a(--x);
};
alert(a);
/*Выподет в ошибку. Так как "a" пока не заданно. Есть только "b" в которой что-то там лежит.*/

function a(x) {
    return x * 2;
}
var a;
alert(a);
/* Выведит тело функции, без её выполнения.*/

function b(x, y, a) {
    arguments[2] = 10;
    alert(a);
}
b(1, 2, 3);
/* Всегда будет выводить 10, так как в самой функции переопределяется аргумент "а".*/


function a() {
    alert(this);
}
a.call(null);
 /*window*/